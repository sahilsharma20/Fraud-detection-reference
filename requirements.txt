# ─────────────────────────────────────────────────────────────────────────────
# FULL dependency set: training + serving + dev/test.
# Pinned versions for reproducibility (a fresher leaves these unpinned and the
# build breaks 3 months later when a transitive dep ships a breaking change).
# Runtime-only deps live in requirements-serve.txt to keep the Docker image small.
# ─────────────────────────────────────────────────────────────────────────────

# --- core scientific stack (numpy pinned <2 for binary compat with shap/xgboost) ---
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
xgboost==2.1.1

# --- data-boundary validation ---
pydantic==2.9.2          # API request/response contracts
pandera==0.20.4          # DataFrame schema validation at the ingestion boundary

# --- experiment tracking & registry ---
mlflow==2.16.2

# --- explainability ---
shap==0.46.0

# --- serving ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4            # server-side templating for the web UI
flask==3.0.3            # alternative web framework (used by flask_app.py + the build guide)
flask-cors==4.0.1       # CORS for the Flask app (if UI is served separately)
gunicorn==23.0.0        # production WSGI server for the Flask app

# --- notebooks (Jupyter ML walkthrough) ---
notebook==7.2.2
nbconvert==7.16.4
nbformat==5.10.4
ipykernel==6.29.5

# --- plotting (EDA + report artifacts) ---
matplotlib==3.9.2
seaborn==0.13.2

# --- config / persistence ---
pyyaml==6.0.2
joblib==1.4.2

# --- dev / test ---
pytest==8.3.3
httpx==0.27.2            # FastAPI TestClient + integration tests
ruff==0.6.8             # linter/formatter (configured in pyproject.toml)
pre-commit==3.8.0
